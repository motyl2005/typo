<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<%= render 'admin/content/merge', {:hidden => @current_user.profile_id != 1 || @_action_name != 'edit', form_action: form_action} %> 